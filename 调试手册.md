# RT-Thread Aicar —— 推箱子智能车调试手册

## 硬件平台

NXP i.MX RT1064 (600MHz) + 三全向轮 + IMU660RA + OpenArt 摄像头
无线串口 2.4GHz (UART8, 115200bps)

---

## 按键

| 按键 | 功能 |
|------|------|
| KEY4 | 启动三关连续任务（Stage1→Stage2→Stage3，全自动无需再按） |

---

## 参数组（`map` 切换，SeekFree 上位机滑块调）

### `map speed` — 电机/速度

| ch | 参数 | 默认(×1000) | 实际值 | 范围(×1000) | 作用 |
|:--:|------|:--:|------|:--:|------|
| 0 | INNER_KP | 100000 | 100.0 | 0~500000 | 轮速环 P 增益 |
| 1 | INNER_KI | 2500 | 2.5 | 0~100000 | 轮速环 I 增益 |
| 2 | INNER_KD | 1000 | 1.0 | 0~50000 | 轮速环 D 增益 |
| 3 | NAV_SPD_MAX | 100 | 0.10 | 10~1000 | 导航最大平动速度 (m/s) |
| 4 | MIN_SPEED | 80 | 0.08 | 10~500 | 最低维持速度 (m/s) |
| 5 | PUSH_SPD_MAX | 80 | 0.08 | 10~1000 | 推箱子最大速度 (m/s) |

### `map angle` — 直行角度环

| ch | 参数 | 默认(×1000) | 实际值 | 范围(×1000) | 作用 |
|:--:|------|:--:|------|:--:|------|
| 0 | ANGL_KP | 1400 | 1.4 | 0~5000 | 直行角度 P 增益 |
| 1 | ANGL_KD | 1400 | 1.4 | 0~3000 | 直行角度 D 增益 |
| 2 | KP_ROT | 1200 | 1.2 | 0~5000 | 识别旋转 P 增益 |

### `map lateral` — 侧移角度环

| ch | 参数 | 默认(×1000) | 实际值 | 范围(×1000) | 作用 |
|:--:|------|:--:|------|:--:|------|
| 0 | LAT_KP | 1400 | 1.4 | 0~5000 | 侧移角度 P 增益 |
| 1 | LAT_KD | 1400 | 1.4 | 0~3000 | 侧移角度 D 增益 |

### `map tracking` — 路径跟踪

| ch | 参数 | 默认(×1000) | 实际值 | 范围(×1000) | 作用 |
|:--:|------|:--:|------|:--:|------|
| 0 | LOOKAHEAD | 150 | 0.15 | 20~500 | 前视距离 (m，当前未生效) |
| 1 | PATH_TOL | 30 | 0.03 | 5~200 | 路径点到达容差 (m) |
| 2 | MIN_LOOKAHD | 80 | 0.08 | 20~300 | 最小前视距离 (m) |
| 3 | MAX_LOOKAHD | 500 | 0.50 | 100~1000 | 最大前视距离 (m) |
| 4 | POS_KP | 2000 | 2.0 | 0~10000 | 位置环 P 增益 |
| 5 | POS_KD | 800 | 0.8 | 0~5000 | 位置环 D 增益 |

---

## 测试命令

| 命令 | 功能 |
|------|------|
| `test empty` | 加载空地图 + 位置归零 |
| `test straight <cm>` | 直行指定距离，打印终点误差 |
| `test lateral <cm>` | 侧移指定距离，打印终点误差 |
| `test square <cm>` | 方形路径一圈，打印闭合误差 |
| `test push <dir> [steps]` | 推 20cm×steps；dir=0(上) 1(右) 2(下) 3(左) |
| `test rotate <deg>` | 原地旋转（RotateToAngleIMU） |
| `test digit` | UART4 数字识别测试 |
| `test box` | UART4 箱子类型识别测试 |

---

## 系统命令

| 命令 | 功能 |
|------|------|
| `map <speed/angle/lateral/tracking>` | 切换 SeekFree 上位机参数组 |
| `status` | 打印当前组所有通道名、值(×1000)、范围 |
| `save` | 打印当前组 C 代码初始化值，方便固化参数 |
| `setpose <x_cm> <y_cm> [yaw_deg]` | 设置小车坐标和航向 |
| `setyaw <deg>` | 手动设置锁定航向（测试用） |
| `reset` | 复位控制器状态（参数保留不变） |
| `help` | 显示帮助 |

---

## 典型调参流程

```
test empty                 → 空地图 + 位置归零
test straight 200          → 直行 2m，观察是否走偏
map angle                  → 上位机切直行角度组，拖 ch0/ch1 调 KP/KD
test lateral 100           → 侧移 1m，观察是否走偏
map lateral                → 上位机切侧移角度组，拖 ch0/ch1 调 KP/KD
test square 100            → 走 1m 方形，验证整体跟踪精度
test rotate 90             → 原地转 90° 测试识别旋转精度
map speed                  → 调速度、轮速 PID
save                       → 打印当前组固化代码
```

## 调试模块开关

`project/code/debug.h` 第 7 行：`#define DEBUG_ENABLE 1` 改为 `0` 彻底禁用。
